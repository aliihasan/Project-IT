// Assignment no 31
let userName = ['admin', 'junaid', 'Ali', 'saas', 'zain'];
if (userName.length === 0) {
    console.log('we need to find some users!');
}
else {
    userName = [];
    console.log('All user name have been removed.' + userName.length);
}
export {};
